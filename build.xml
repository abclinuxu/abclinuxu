<project name="AbcLinuxu" basedir="." default="deploy">

  <property name="src" value="src" />
  <property name="data" value="../data-abc" />
  <property name="tomcat" value="/home/literakl/tomcat" />
  <property name="build" value="/home/literakl/ROOT" />
  <property name="backup" value="/home/literakl/tmp/ven" />

  <path id="project.classpath">
    <fileset dir="libs">
      <include name="*.jar" />
    </fileset>
    <fileset dir="${tomcat}/common/lib">
      <include name="activation.jar" />
      <include name="mail.jar" />
    </fileset>
  </path>


  <target name="init">

    <mkdir dir="${build}" />
    <mkdir dir="${build}/WEB-INF" />
    <mkdir dir="${build}/WEB-INF/lib" />
    <mkdir dir="${build}/WEB-INF/classes" />
    <mkdir dir="${build}/WEB-INF/templates" />
    <mkdir dir="${build}/WEB-INF/logs" />
    <mkdir dir="${build}/auto" />
    <tstamp />

  </target>


  <target name="clean">

    <delete dir="${build}/WEB-INF/classes" />

  </target>


  <target name="build" depends="init">

    <javac srcdir="src" destdir="${build}/WEB-INF/classes" classpathref="project.classpath" debug="on" />
    <javac srcdir="test" destdir="${build}/WEB-INF/classes" classpathref="project.classpath" />

  </target>


  <target name="deploy" depends="build">

    <copy todir="${build}/WEB-INF">
      <fileset dir="conf">
        <include name="web.xml" />
        <include name="templates.xml" />
        <include name="log4j.xml" />
        <include name="velocity.properties" />
      </fileset>
    </copy>

    <copy todir="${build}/WEB-INF/templates">
      <fileset dir="web/templates" />
      <fileset dir="conf" includes="macros.vm" />
    </copy>

    <copy todir="${build}/ikony">
      <fileset dir="web/ikony" />
    </copy>

    <copy todir="${build}/images">
      <fileset dir="${data}/web/images" />
    </copy>

    <copy todir="${build}">
      <fileset dir="${data}/web/root" />
    </copy>

    <copy todir="${build}/WEB-INF/lib">
      <fileset dir="libs" includes="*.jar">
        <exclude name="servlet.jar" />
        <exclude name="crimson.jar" />
      </fileset>
    </copy>

  </target>

</project>