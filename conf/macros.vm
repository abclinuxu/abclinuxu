#macro(sync $object)
#if(!$object.isInitialized()) $PERSISTANCE.synchronize($object)#end
#end

#macro(parents)
#if($RELATION)
<div class="parents">
#foreach($parent in $PARENTS)
#if($velocityCount==1) #sync($parent.Parent)
<a href="$!URL.constructURL("/ViewCategory?categoryId=$parent.Parent.Id")">$!UTIL.getXPath($parent.Parent,'data/name')</a>
#end
- <a href="$!URL.constructURL("/ViewRelation?relationId=$parent.Id")">$UTIL.getChildName($parent)</a>
#end</div>#end#end
