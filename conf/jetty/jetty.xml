<?xml version="1.0"  encoding="ISO-8859-1"?> 
<!DOCTYPE Configure PUBLIC "-//Mort Bay Consulting//DTD Configure 1.2//EN" "http://jetty.mortbay.org/configure_1_2.dtd">



<!-- =============================================================== -->
<!-- Configure the Jetty Server                                      -->
<!-- =============================================================== -->
<Configure class="org.mortbay.jetty.Server">

  <!-- =============================================================== -->
  <!-- Configure the Request Listeners                                 -->
  <!-- =============================================================== -->
  <Call name="addListener">
    <Arg>
      <New class="org.mortbay.http.SocketListener">
        <Set name="Port"><SystemProperty name="jetty.port" default="8080"/></Set>
        <Set name="MinThreads">15</Set>
        <Set name="MaxThreads">200</Set>
        <Set name="MaxIdleTimeMs">15000</Set>
        <Set name="LowResourcePersistTimeMs">5000</Set>
      </New>
    </Arg>
  </Call>

  <!-- =============================================================== -->
  <!-- Configure the Contexts                                          -->
  <!-- =============================================================== -->

  <Call name="addWebApplication">
    <Arg>/</Arg>
    <Arg>/var/www/deploy/abclinuxu</Arg>

    <Set name="defaultsDescriptor">org/mortbay/jetty/servlet/webdefault.xml</Set>
  </Call>

  <!-- =============================================================== -->
  <!-- Configure the Request Log                                       -->
  <!-- =============================================================== -->
  <Set name="RequestLog">
    <New class="org.mortbay.http.NCSARequestLog">
      <Arg>/var/www/deploy/logs/yyyy_mm_dd.request.log</Arg>
      <Set name="retainDays">90</Set>
      <Set name="append">true</Set>
      <Set name="extended">true</Set>
      <Set name="buffered">false</Set> <!-- jetty perf. tuning: server with buffering turned on has around 5% maximum throughput -->
      <Set name="LogTimeZone">GMT</Set>
      <Set name="ignorePaths">
        <Array type="String">
          <Item>/images/*</Item>
          <Item>*.css</Item>
          <Item>*.js</Item>
        </Array>
      </Set>
    </New>
  </Set>

</Configure>
